[Documentation Home](../../README.md) > [skills](../README.md) > [task-manager](./README.md) > **task-manager**

---

# 📁 task-manager

> **Purpose:** Contains the documentation-level source index and a TypeScript source submodule for the task-manager skill, including core modules and type definitions used by the skill and a UI submodule.
> 

![Organization: Hierarchical](https://img.shields.io/badge/Organization-Hierarchical-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Subdirectories](#subdirectories)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)

---

## Overview

This directory documents and indexes the task-manager skill and points to the TypeScript source contained in its src/ subdirectory. At the top level this directory is intended to describe and organize the skill's implementation: the src/ subdirectory holds six root TypeScript modules (index.ts, types.ts, store.ts, sanitise.ts, id.ts, evidence.ts). Those files provide the core modules and typed contracts used by the skill and a UI submodule; their filenames indicate intent (for example types.ts is meant to contain type definitions used across the skill).

The src/ subdirectory is the canonical location for the skill implementation. Together the files in src/ implement the skill surface — index.ts acts as the primary module entry, types.ts centralises type definitions, store.ts handles persistent or in-memory data store concerns, sanitise.ts deals with input sanitisation, id.ts contains identity/ID utilities, and evidence.ts captures evidence-related logic. This directory's role in the larger system is to group the task-manager skill's source and signal to developers where to find the core modules and type definitions when integrating or maintaining the skill within the broader codebase.


### File Organization

The directory is organized with a single src/ subdirectory that contains the TypeScript implementation files. Files in src/ are modular TypeScript modules, named to reflect their responsibilities (index.ts as entry, types.ts for type definitions, store.ts for storage logic, sanitise.ts for input sanitisation, id.ts for identifier utilities, and evidence.ts for evidence handling). This keeps source code separated from documentation and higher-level README material.

## 📂 Subdirectories

This directory contains the following subdirectories:

### [📁 src](./src/README.md)

**Purpose:** Holds the TypeScript source files for the task-manager skill, including core modules and type definitions used by the skill and a UI submodule.

![Files: 6](https://img.shields.io/badge/Files-6-blue)

---
## Dependencies

### Internal Dependencies

| Dependency | Usage |
| --- | --- |
| `src modules` | Local TypeScript modules within src/ are used to provide the core skill implementation and shared type definitions. |

## Architecture Notes

- The skill is implemented as a set of modular TypeScript files under src/ with a clear entry point (index.ts) and separated concerns (types, store, sanitise, id, evidence).
- Type definitions are centralized in types.ts to maintain consistent contracts between the skill's modules and its UI submodule.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)
**🔗 Related:** [src](./src/README.md)

---

*Generated by Woden Docbot*
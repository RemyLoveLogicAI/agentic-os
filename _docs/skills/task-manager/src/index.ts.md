<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "skills/task-manager/src/index.ts",
  "source_hash": "9a638186a1c2cc9bf96bbce5905e8dbf1a1d15b26ea3be3df44970ba5181b274",
  "last_updated": "2026-05-19T15:42:18.176623+00:00",
  "tokens_used": 4417,
  "complexity_score": 2,
  "estimated_review_time_minutes": 5,
  "external_dependencies": []
}
```

</details>

[Documentation Home](../../../README.md) > [skills](../../README.md) > [task-manager](../README.md) > [src](./README.md) > **index**

---

# index.ts

> **File:** `skills/task-manager/src/index.ts`

![Complexity: Low](https://img.shields.io/badge/Complexity-Low-green) ![Review Time: 5min](https://img.shields.io/badge/Review_Time-5min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Usage Examples](#usage-examples)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This file is a lightweight index that aggregates and re-exports the package's public API so consumers can import from "@agentic-os/task-manager" instead of deep internal paths. It exposes runtime symbols (TaskStore, createTaskStore, KanbanDesk, onTaskEvent, emitTaskEvent, sanitiseString, sanitiseOptionalString, sanitiseStringArray, KANBAN_COLUMNS) and TypeScript-only type exports.

The file contains no implementation logic — only explicit export statements that reference internal modules with .js extensions to align with ESM build output.

## Dependencies

### Internal Dependencies

| Module | Usage |
| --- | --- |
| [./store.js](.././store.js.md) | Re-exports runtime exports TaskStore and createTaskStore so consumers can construct or use the core task store API. Exact export line: `export { TaskStore, createTaskStore } from "./store.js";` |
| [./ui/kanban-desk.js](.././ui/kanban-desk.js.md) | Re-exports the KanbanDesk UI facade class for use by callers. Exact export line: `export { KanbanDesk } from "./ui/kanban-desk.js";` |
| [./evidence.js](.././evidence.js.md) | Re-exports event/evidence helpers onTaskEvent and emitTaskEvent. Exact export line: `export { onTaskEvent, emitTaskEvent } from "./evidence.js";` |
| [./sanitise.js](.././sanitise.js.md) | Re-exports sanitisation utilities sanitiseString, sanitiseOptionalString, sanitiseStringArray for downstream consumers. Exact export line: `export { sanitiseString, sanitiseOptionalString, sanitiseStringArray } from "./sanitise.js";` |
| [./types.js](.././types.js.md) | Re-exports a set of TypeScript types (Task, TaskCreateInput, TaskUpdateInput, TaskMoveInput, TaskListFilter, KanbanBoard, KanbanColumn, TaskEvent, TaskStoreOptions, PersistMode) as type-only exports and also re-exports the runtime constant KANBAN_COLUMNS. Exact export lines: `export type { Task, TaskCreateInput, TaskUpdateInput, TaskMoveInput, TaskListFilter, KanbanBoard, KanbanColumn, TaskEvent, TaskStoreOptions, PersistMode, } from "./types.js";` and `export { KANBAN_COLUMNS } from "./types.js";` |

## 📁 Directory

This file is part of the **src** directory. View the [directory index](_docs/skills/task-manager/src/README.md) to see all files in this module.

## Architecture Notes

- Barrel (re-export) pattern: centralizes public API surface so consumers import from a single package entrypoint instead of internal paths.
- Separation of runtime exports and type-only exports: types are exported using `export type { ... }` to avoid adding runtime import overhead or circular dependencies.
- Use of .js file extensions in module specifiers indicates this file targets ESM-compatible output (TypeScript compiled to .js files) and must align with tsconfig/module resolution at build time.

## Usage Examples

### Main use case

Consumer imports createTaskStore and KanbanDesk from the package to create a persistent store and construct a Kanban UI facade; then uses KanbanDesk methods (for example: addTask, moveTask, getBoard) to manage tasks. This example is shown in the file header comment and relies on the re-exports: createTaskStore and KanbanDesk.

## Maintenance Notes

- Keep exported symbol lists in sync with the implementations in the referenced modules (store.js, ui/kanban-desk.js, evidence.js, sanitise.js, types.js).
- If build output or module resolution changes (e.g., removing .js extensions), update the specifiers to match emitted files to avoid runtime import errors.
- Types are exported with `export type { ... }` — do not convert them to value exports unless the corresponding runtime values exist in ./types.js.

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/skills/task-manager/src/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*

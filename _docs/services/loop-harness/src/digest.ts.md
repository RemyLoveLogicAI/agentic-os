<!-- METADATA: {"source_path": "services/loop-harness/src/digest.ts", "source_sha": "", "extraction_quality": "regex_fallback", "model": "gpt-5-mini", "generated_at": "2026-07-18T20:50:57Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "services/loop-harness/src/digest.ts",
  "source_hash": "92e1fc36e450a534e99768fd6875c0567883f17c7da1f3b223eb33be92ff948a",
  "last_updated": "2026-07-18T20:50:46.997214+00:00",
  "tokens_used": 1268,
  "complexity_score": 1,
  "estimated_review_time_minutes": 5,
  "external_dependencies": [
    "import { createHash } from \"node:crypto\";",
    "import { readdirSync, readFileSync, statSync } from \"node:fs\";",
    "import { join, relative } from \"node:path\";"
  ]
}
```

</details>

[Documentation Home](../../../README.md) > [services](../../README.md) > [loop-harness](../README.md) > [src](./README.md) > **digest**

---

# digest.ts

> **File:** `services/loop-harness/src/digest.ts`

![Complexity: Low](https://img.shields.io/badge/Complexity-Low-green) ![Review Time: 5min](https://img.shields.io/badge/Review_Time-5min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This TypeScript module provides a single utility function named walk. The file imports Node core modules for cryptographic hashing (node:crypto), filesystem operations (node:fs), and path manipulation (node:path), indicating that the walk function is responsible for traversing directories, reading files, and producing path- and content-related outputs (for example, relative paths and content hashes) using synchronous filesystem APIs. The implementation style centers on synchronous, Node.js core APIs rather than async/promises or third‑party libraries.

Overall, the module serves as a small, focused helper for filesystem traversal and processing, exposing one module-level function that likely combines directory walking with file reading and hashing/path normalization tasks for downstream consumers.

## Dependencies

### External Dependencies

| Module | Usage |
| --- | --- |
| `import { createHash } from "node:crypto";` | import { createHash } from "node:crypto"; |
| `import { readdirSync, readFileSync, statSync } from "node:fs";` | import { readdirSync, readFileSync, statSync } from "node:fs"; |
| `import { join, relative } from "node:path";` | import { join, relative } from "node:path"; |

## 📁 Directory

This file is part of the **src** directory. View the [directory index](_docs/services/loop-harness/src/README.md) to see all files in this module.

## Architecture Notes

- Uses Node.js core modules exclusively (node:crypto, node:fs, node:path).
- Implements a single synchronous utility function (walk) at module level.
- File I/O is performed with synchronous APIs (readdirSync, readFileSync, statSync).
- Documentation generated from regex-based extraction for TypeScript; class/function detection is best-effort.

## Maintenance Notes

- Provide a module-level function walk that traverses filesystem directories.
- Read file contents and metadata using synchronous Node.js fs APIs (readdirSync, readFileSync, statSync).
- Compute or prepare content-based data using the Node.js crypto createHash utility.
- Normalize and compute file paths using Node.js path utilities (join, relative).

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/services/loop-harness/src/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*


---

## Functions and Classes


#### walk

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```typescript
def walk(dir, root, acc):
```

### Description

Recursively traverses the filesystem starting at the given directory and gathers information about files and/or subdirectories into an accumulator.

Recursively traverses the filesystem starting at the given directory and gathers information about files and/or subdirectories into an accumulator. It uses the provided root to determine base or relative paths while walking the tree.


dir is the directory path to walk; root is the base path used for computing relative paths or context during the traversal; acc is an accumulator (such as an array or object) that is populated with the discovered entries as the walk proceeds.

Returns the populated accumulator containing the collected entries from the directory walk.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `dir` | `Unknown` | ✅ | Parameter dir |
| `root` | `Unknown` | ✅ | Parameter root |
| `acc` | `Unknown` | ✅ | Parameter acc |

### Complexity

Not analyzed

---



<!-- METADATA: {"source_path": "services/loop-harness/src/distribution.ts", "source_sha": "", "extraction_quality": "regex_fallback", "model": "gpt-5-mini", "generated_at": "2026-07-18T20:50:57Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "services/loop-harness/src/distribution.ts",
  "source_hash": "952818bf8ed863abd31403d4f0ca224594260fdbd9b8b337ec8306e180eb0b40",
  "last_updated": "2026-07-18T20:50:44.816741+00:00",
  "tokens_used": 812,
  "complexity_score": 1,
  "estimated_review_time_minutes": 5,
  "external_dependencies": [
    "import { cpSync, mkdirSync, rmSync, writeFileSync, existsSync } from \"node:fs\";",
    "import { isAbsolute, join, normalize } from \"node:path\";",
    "import type { ExecutionState, Project } from \"./types.js\";",
    "import type { StoragePaths } from \"./storage.js\";"
  ]
}
```

</details>

[Documentation Home](../../../README.md) > [services](../../README.md) > [loop-harness](../README.md) > [src](./README.md) > **distribution**

---

# distribution.ts

> **File:** `services/loop-harness/src/distribution.ts`

![Complexity: Low](https://img.shields.io/badge/Complexity-Low-green) ![Review Time: 5min](https://img.shields.io/badge/Review_Time-5min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This TypeScript module appears to be focused on filesystem-level distribution tasks within a larger service. It imports synchronous Node.js filesystem primitives (cpSync, mkdirSync, rmSync, writeFileSync, existsSync) and path utilities (isAbsolute, join, normalize) suggesting it handles copying, creating, removing, and writing files while normalizing and resolving paths. The file also references type definitions for ExecutionState, Project, and StoragePaths, indicating it works in the context of execution metadata, project configuration, and storage layout, although no concrete functions or classes are exposed in the extracted summary.

Given the imports and type ties, the module likely coordinates where artifacts are placed and how paths are resolved when distributing or preparing project data on disk. The lack of extracted functions or classes implies the current summary is limited to the module's dependencies and intended role rather than implementation details.

## Dependencies

### External Dependencies

| Module | Usage |
| --- | --- |
| `import { cpSync, mkdirSync, rmSync, writeFileSync, existsSync } from "node:fs";` | import { cpSync, mkdirSync, rmSync, writeFileSync, existsSync } from "node:fs"; |
| `import { isAbsolute, join, normalize } from "node:path";` | import { isAbsolute, join, normalize } from "node:path"; |
| `import type { ExecutionState, Project } from "./types.js";` | import type { ExecutionState, Project } from "./types.js"; |
| `import type { StoragePaths } from "./storage.js";` | import type { StoragePaths } from "./storage.js"; |

## 📁 Directory

This file is part of the **src** directory. View the [directory index](_docs/services/loop-harness/src/README.md) to see all files in this module.

## Architecture Notes

- Uses Node.js synchronous fs APIs (cpSync, mkdirSync, rmSync, writeFileSync, existsSync)
- Uses Node.js path utilities for normalization and joining of paths
- Imports and relies on TypeScript type-only imports for ExecutionState, Project, and StoragePaths
- Documentation generated from regex-based extraction for TypeScript; class/function detection is best-effort.

## Maintenance Notes

- Perform synchronous filesystem operations such as copying, creating directories, removing files/directories, writing files, and checking existence using Node.js fs APIs.
- Normalize and resolve filesystem paths, including joining path segments and checking whether paths are absolute.
- Interact with the surrounding system via the ExecutionState, Project, and StoragePaths types to determine what to distribute and where on disk.
- Coordinate placement and preparation of project artifacts on disk according to storage path conventions (implied by StoragePaths).

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/services/loop-harness/src/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*

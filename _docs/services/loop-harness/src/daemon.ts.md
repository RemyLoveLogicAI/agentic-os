<!-- METADATA: {"source_path": "services/loop-harness/src/daemon.ts", "source_sha": "", "extraction_quality": "regex_fallback", "model": "gpt-5-mini", "generated_at": "2026-07-18T20:50:57Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "services/loop-harness/src/daemon.ts",
  "source_hash": "8420a73da15158a948f844c347ee1f5d742f90cd1ff940ed9a86ba704a0f02a6",
  "last_updated": "2026-07-18T20:50:46.350997+00:00",
  "tokens_used": 1217,
  "complexity_score": 1,
  "estimated_review_time_minutes": 6,
  "external_dependencies": [
    "import { mkdirSync, readdirSync, readFileSync, renameSync, rmSync, writeFileSync } from \"node:fs\";",
    "import { basename, join } from \"node:path\";",
    "import { randomUUID } from \"node:crypto\";",
    "import type { StoragePaths } from \"./storage.js\";",
    "import type { ProjectConfig } from \"./config.js\";",
    "import { parseConfig, projectFromConfig } from \"./config.js\";",
    "import { runLoop } from \"./loop.js\";"
  ]
}
```

</details>

[Documentation Home](../../../README.md) > [services](../../README.md) > [loop-harness](../README.md) > [src](./README.md) > **daemon**

---

# daemon.ts

> **File:** `services/loop-harness/src/daemon.ts`

![Complexity: Low](https://img.shields.io/badge/Complexity-Low-green) ![Review Time: 6min](https://img.shields.io/badge/Review_Time-6min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This TypeScript module provides a small daemon harness around a core "loop" runner and project configuration utilities. It imports filesystem and path helpers, a UUID generator, and typed configuration/storage interfaces, and exposes a delay utility function. The module coordinates configuration parsing and project construction (via parseConfig and projectFromConfig) and ultimately hands control to a runLoop function, while using low-level node:fs and node:path operations for file manipulation where needed.

## Dependencies

### External Dependencies

| Module | Usage |
| --- | --- |
| `import { mkdirSync, readdirSync, readFileSync, renameSync, rmSync, writeFileSync } from "node:fs";` | import { mkdirSync, readdirSync, readFileSync, renameSync, rmSync, writeFileSync } from "node:fs"; |
| `import { basename, join } from "node:path";` | import { basename, join } from "node:path"; |
| `import { randomUUID } from "node:crypto";` | import { randomUUID } from "node:crypto"; |
| `import type { StoragePaths } from "./storage.js";` | import type { StoragePaths } from "./storage.js"; |
| `import type { ProjectConfig } from "./config.js";` | import type { ProjectConfig } from "./config.js"; |
| `import { parseConfig, projectFromConfig } from "./config.js";` | import { parseConfig, projectFromConfig } from "./config.js"; |
| `import { runLoop } from "./loop.js";` | import { runLoop } from "./loop.js"; |

## 📁 Directory

This file is part of the **src** directory. View the [directory index](_docs/services/loop-harness/src/README.md) to see all files in this module.

## Architecture Notes

- Uses low-level Node.js filesystem and path APIs for file operations.
- Separates concerns by delegating configuration parsing and loop execution to imported modules (parseConfig, projectFromConfig, runLoop).
- Includes a small asynchronous utility (delay) to support timing or backoff in the daemon.
- Documentation generated from regex-based extraction for TypeScript; class/function detection is best-effort.

## Maintenance Notes

- Provide a delay helper function for use by other asynchronous operations in the daemon.
- Coordinate parsing of project configuration using parseConfig and creation of project instances via projectFromConfig.
- Invoke the main loop runner by calling runLoop to start the daemon behavior.
- Use Node filesystem and path primitives (mkdirSync, readdirSync, readFileSync, renameSync, rmSync, writeFileSync, basename, join) to manage files and directories as part of daemon duties.

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/services/loop-harness/src/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*


---

## Functions and Classes


#### delay

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```typescript
def delay(ms, signal?):
```

### Description

Pause execution for a specified number of milliseconds, resolving after the timeout elapses.

Pause execution for a specified number of milliseconds, resolving after the timeout elapses. It supports an optional abort mechanism to cancel the wait early.


ms is the delay duration in milliseconds. signal is an optional abort signal (e.g., an AbortSignal) that can be used to cancel the pending delay before it completes.

Returns a Promise that resolves when the delay completes or rejects/terminates early if the provided abort signal triggers cancellation.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `ms` | `Unknown` | ✅ | Parameter ms |
| `signal?` | `Unknown` | ✅ | Parameter signal? |

### Complexity

Not analyzed

---



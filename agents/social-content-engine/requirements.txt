anthropic>=0.40.0,<1.0.0
langchain-anthropic>=0.3.0,<2.0.0
langchain-core>=0.3.0,<2.0.0
langgraph>=0.2.0,<2.0.0
httpx>=0.27.0,<1.0.0

# Optional checkpoint backends — install separately based on deployment target:
#   DynamoDB:  pip install langgraph-checkpoint-dynamodb
#   Postgres:  pip install langgraph-checkpoint-postgres
#   SQLite (default, zero config): included in langgraph
